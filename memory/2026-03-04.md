## Activity Log → Memory Section Transition
- Michael wants to REMOVE the Activity Log card from dashboard
- Replace with a **Memory section** for:
  - Conversation journals (summaries of what we discussed each day)
  - Key insights (preferences, decisions, context to remember)
  - Learning notes (things I'm getting better at, mistakes to avoid)
  - Running context (ongoing projects, status, next steps)
- Activity Log has been inconsistent/problematic - needs manual prompting
- Memory section would be more valuable for persistence

---

# 2026-03-04 - Notes Fix + Persistent Memory Discussion

## PR #172 - Edit Trade Notes Fix (Pushed)
- Fixed Exit Notes not persisting when blank and saved
- Changed `undefined` to `null` for `entryNotes` and `exitNotes` in handleSubmit
- This ensures notes are included in JSON.stringify and sent in PUT request
- Branch: `fix/trade-notes-display` → PR #172
- Commit: `22e2314`

## PR #169 - Remove Analytics (Updated)
- Deleted `components/trading/AnalyticsView.tsx` completely
- Removes ALL Analytics code from codebase
- Branch force-pushed: `fix/remove-analytics-tab`
- Commit: `889438e`

## PR #171 - Monthly PnL Stats Redesign (Updated)
- All 4 stats now in uniform badge style: Monthly PnL, Trading Days, Win/Loss, Total Trades
- Desktop: Single stats card with 4 badges separated by dividers
- Mobile: Horizontal row with all 4 stats in consistent format
- Branch: `feature/monthly-pnl-calendar`
- Commit: `9e50c22`

## Persistent Memory Discussion
- Michael shared Mem0 video (https://youtu.be/Nt03hgxv5TE)
- Mem0 = universal AI memory layer for LLM applications
- Alternative: Native SQLite for local persistent memory
- Decision pending on approach

## Daily Habit Record Cron Issue
- Cron tried to auto-record habits at 11:59 PM EST
- Failed with 404 - `/api/habits/daily-record` endpoint not deployed
- Habits feature needs to be merged to main and deployed to Vercel

---

# 2026-03-03 - Trading Dashboard Updates

## Timezone Bug Fix (Trading)
- Fixed API route `app/api/trades/[id]/route.ts` to use EST timezone
- Changed `new Date().toISOString()` to `getNowInEST()` for timestamps
- This fixes trades showing wrong date when updated from Trade Management
- Commit: `f753dd6`

## Add to Calendar Button Criteria
- Button is HIDDEN when:
  - Position was already added in current session (`addedToCalendarIds`)
  - OR trade with same symbol AND date exists in calendar trades
- Button is SHOWN when position hasn't been added yet
- Validation in `handleConfirmAddToCalendar` catches duplicates

## Edit Trade Feature (CalendarView)
- Added edit button to each trade row in All Trades table
- Created `EditTradeModal` component with form fields:
  - Symbol, Side (dropdown), Shares, Entry Price
  - Entry Date (datetime picker)
  - Exit Price (optional), Status (OPEN/CLOSED dropdown)
- `handleSaveTrade` function updates via PUT `/api/trades/:id`
- Allows changing trade status directly from Overview
- Commit: `e278c84`

## User Context
- User (Michael) wants ALL times in the app to be in EST
- Some trades showing as OPEN when they should be CLOSED
- Need to ensure timezone consistency across all trading features

## IMPORTANT - Pre-Commit Checklist (Added 2026-03-03)
After multiple syntax errors tonight, I MUST verify before declaring "ready":

1. **Run TypeScript check**: `npx tsc --noEmit components/trading/WatchlistView.tsx`
2. **Run full build**: `npm run build` (catches JSX errors tsc misses)
3. **Check Vercel dashboard** for build status
4. **Only then** say "ready for rebuild"

Never skip these steps! Syntax errors in production are embarrassing and waste time.

## Favorites Feature
- Implemented Favorites section in Potential Trades
- Shows favorited trades at top with yellow header
- Duplicate ticker validation prevents same symbol in favorites
- Other Trades section below with divider
- Profit shows absolute value (positive for both long/short)
- Removed non-functional "+ Add notes" button

## Calendar Date Grouping Fix
- Fixed daily-stats API to group CLOSED trades by exitDate instead of entryDate
- This ensures trades show on correct date when added from Trade Management

## Multi-Select for Closed Positions (COMPLETE)
- Added state for `selectedClosedPositions` (Set)
- Added `showDeleteMultipleModal` and `isDeletingMultiple` states
- Added toggle functions for selection
- Added bulk delete functionality
- Added checkboxes to each position card
- Added "Select All" and "Delete Selected" buttons to header
- Commit: `5fd090d`

## Subagent Research Goal
- Created script at `scripts/add-subagent-goal.js` to add goal to Redis
- Direct Redis connection not available from this environment
- Goal can be added via script or manually through Goals UI
- Full research document at `docs/SUBAGENT_RESEARCH.md`

## Subagents Completed (2026-03-03 04:10 UTC)
Michael assigned 4 tasks, all completed:

1. **Task 1: Habits Fix** ✅ COMPLETE
   - Fixed PR #155 merge conflicts
   - Implemented habits persistence
   - Auto-submit at 12am EST
   - Created streaks/report view
   - Branch: `pr-155`

2. **Task 2: Remove Analytics** ✅ COMPLETE
   - Removed Analytics tab from Trading
   - Cleaned up all related code
   - Deleted AnalyticsView.tsx and API route

3. **Task 3: New Features** ✅ COMPLETE
   - Memory Tab (conversation journal)
   - Projects Tab (replace Tasks)
   - Team Page (subagent management)
   - Branch: `feature/dashboard-memory-projects-team`

4. **Task 4: Subagent Research** ✅ COMPLETE
   - Research documented in `docs/SUBAGENT_RESEARCH.md`
   - Created collaborative goal
   - 5-step implementation plan ready

## Merges to Main (2026-03-03 Evening)
- **feature/trading-calculator-v2** → `main`
  - Multi-select for Closed Positions
  - Trade notes display in Overview
  - Commit: `a1cfd7f`

## Search Filter for Potential Trades
- Branch: `feature/potential-trades-search`
- Added search input to filter by ticker/symbol
- Case-insensitive filtering
- Clear search button functionality
- Pushed and ready

## Expandable Notes Modal
- Branch: `fix/trade-notes-display`
- Added click-to-expand functionality for trade notes
- Created modal showing:
  - Entry Notes section (if present)
  - Exit Notes section (if present)
  - Empty state when no notes
- Full notes visible with whitespace preservation
- Scrollable for long notes (max 60vh)
- Commit: `7eb6c15`
- **PR #163 created and ready for review**

## Notes Not Saving Bug (Subagent Investigation)
- Subagent deployed to investigate notes not updating on Active Trades
- **Root Cause:** Two bugs in `WatchlistView.tsx`:
  1. `undefined.toString()` error when trade had no notes
  2. UI conditional rendering prevented editing when notes were empty
- **Fix:** Always render clickable notes section with visual distinction
- Branch: `fix/notes-not-saving-active-trades`
- Commit: `bbd6b20`

## Edit Active Trade - Shares Manual Override
- User needed to manually adjust shares when actual entry differs from planned
- Shares field was `readOnly` with "auto-calculated" label
- **Changes:**
  - Made shares field editable
  - Added `sharesManuallyEdited` state to track overrides
  - Auto-calculation only when not manually edited
  - "Manual" badge (yellow) vs "Auto-calculated" badge (blue)
  - "Reset to Auto" button to recalculate based on risk parameters
- Branch: `feature/edit-active-trade-shares`
- Commit: `a3932f5`
- **PR #165 created and ready for review**

## Open PRs Summary (End of Day)
1. **PR #163** - Expandable trade notes modal
2. **PR #164** - Notes not saving fix (subagent work)
3. **PR #165** - Editable shares in Edit Active Trade modal
